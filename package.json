{
  "name": "solar-atlas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An interactive 3D solar system spanning 10000 BCE to 10000 CE.",
  "scripts": {
    "start": "node tools/serve.mjs",
    "test": "node --test tests/orbits.test.mjs",
    "check": "node --check app.js && node --check timeline.js && node --check orbits.js && node --check tools/serve.mjs"
  },
  "engines": { "node": ">=18" }
}
